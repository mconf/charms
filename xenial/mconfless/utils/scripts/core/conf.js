/**
 * @name Conf
 *
 * @desc Set of configurations
 */

const config = require('../conf/config.json')
const label = require('../conf/label.js')

module.exports = {
  config: config,
  label: label
}
