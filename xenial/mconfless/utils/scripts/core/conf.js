/**
 * @name Conf
 *
 * @desc Set of configurations
 */

const config = require('../conf/config.json')
const label = require('../conf/label.json')

module.exports = {
  config: config,
  label: label
}
